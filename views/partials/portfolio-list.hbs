<div class="portfolio-list">
    <ul>
        {{#each portfolioItems}}
        <li>
            <a href="{{this.link}}" class="{{#if (eq this.link ../currentPath)}}active{{/if}}">
                {{this.title}}
            </a>
        </li>
        {{/each}}
    </ul>
</div>